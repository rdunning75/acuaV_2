android {
            defaultConfig {
                applicationId = "gov.lacounty.acua"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }